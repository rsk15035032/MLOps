apiVersion: v1
kind: Service
metadata:
  name: churn-service
spec:
  type: NodePort
  selector:
    app: churn-prediction
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080