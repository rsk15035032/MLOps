[project]
name = "churn-prediction-lambda"
version = "1.0.0"
description = "Production-ready churn prediction using AWS Lambda"
requires-python = ">=3.13"

dependencies = [
    "scikit-learn>=1.7.1"
]